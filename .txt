auto-pr-writer/
│
├── app/
│   ├── main.py              # FastAPI entry
│   ├── github.py            # GitHub webhook handling
│   ├── diff_parser.py       # Parse & summarize git diff
│   ├── classifier.py        # Classify PR type
│   ├── pr_writer.py         # LLM prompt + generation
│   └── schemas.py           # Pydantic models
│
├── .env
├── requirements.txt
└── README.md


-----ngrok setup ----------------------------------------------

curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc \
| sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null


echo "deb https://ngrok-agent.s3.amazonaws.com buster main" \
| sudo tee /etc/apt/sources.list.d/ngrok.list


sudo apt update
sudo apt install ngrok


ngrok config add-authtoken 2q0SDcTGm829zf4ionONOsh1Lgp_24xHwSpWgH984hEHCWucF

ngrok http 8000
----------------------------------------------------------------
